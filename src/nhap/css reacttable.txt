/* CSS */
 .react-table-container {
  position: 'absolute';
  max-height: 200px;
  max-width: 800px;
  overflow-x: auto;
  overflow-y: auto;
  /* Enable vertical scrolling */
} 

.react-table {
  background-color: white;
  border: 1px solid #ccc;
  white-space: nowrap;
  width: inherit;

}

.react-table table {
  border-collapse: collapse;
  width: 100%;
}

.react-table th,
.react-table td {
  border: 1px solid #ccc;
  padding: 8px;
}



.react-table th {
  background-color: #f2f2f2;
  text-align: left;
  font-weight: bold;
  text-align: center;
  top: 0;
  z-index: 0;
  position: sticky;
}

.react-table td {
  /* background-color: white;khong hightlight duoc khi de */
  overflow: hidden; /* Ẩn phần nội dung vượt ra ngoài giới hạn của ô */
  text-overflow: ellipsis; /* Hiển thị dấu ba chấm khi nội dung bị ẩn */
  border: 1px solid #ccc;
  padding: 5px;
  z-index: 0;
}

.react-table tr:hover {
  background-color: #f5f5f5;
  /* Màu nền highlight */
}
.react-table-highlighted {
  text-decoration-color: aquamarine;
  background-color: #f5f5f5;
  /* Màu nền highlight */
}

/* .react-table-row {
  background-color: #a70c0c
} */

/* Phần resizer */
.react-table-resizer {
  display: inline-block;
  position: absolute;
  right: 0;
  bottom: 0;
  width: 2px;
  /* Độ rộng của resizer */
  height: 100%;
  /* Chiều cao của resizer */
  background: transparent;
  /* Màu nền của resizer */
  transform: translateX(50%);
  */ z-index: 1;
  prevent scroll on touch devices touch-action: none;
  cursor: col-resize;

  /* Con trỏ khi di chuột qua resizer
}

/* Phần isResizing */
  .react-table-isResizing {
    background-color: #f0f0f0;
    /* Màu nền khi đang thay đổi kích thước */
  }